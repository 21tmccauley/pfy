"""Failing-validator triage capability.

The typed I/O contract lives in ``models.py``. The deterministic engine (bundle
assembly, regex/rule replay, passing-vs-failing diff) is being extracted from the
two prototypes into ``bundle.py`` / ``replay.py`` / ``diff.py`` — see models.py.
"""
