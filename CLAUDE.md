# pfy

Guidance for driving this CLI as an agent lives in @AGENTS.md — read it before
running `pfy` (the output contract, exit-code semantics, when to use porcelain vs
plumbing vs the `api` escape hatch, and the validator-triage / MCP surfaces).
